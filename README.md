# EdgeSKD
A self-constructed 3D dental dataset and a self-annotated ShapeNet subset for keypoint detection

Here, we only provide a point cloud and the corresponding JSON file, and run demo.py for visualization.

The complete dataset will be made public after the paper is accepted.
